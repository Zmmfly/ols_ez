#ifndef MAXMINDDB_CONFIG_H
#define MAXMINDDB_CONFIG_H

#ifndef MMDB_UINT128_USING_MODE
/* Define as 1 if we use unsigned int __atribute__ ((__mode__(TI))) for uint128 values */
#define MMDB_UINT128_USING_MODE 0
#endif

#ifndef MMDB_UINT128_IS_BYTE_ARRAY
/* Define as 1 if we don't have an unsigned __int128 type */
#undef MMDB_UINT128_IS_BYTE_ARRAY
#endif

#endif                          /* MAXMINDDB_CONFIG_H */

#define PACKAGE "libmaxminddb"
#define PACKAGE_BUGREPORT "support@maxmind.com"
#define PACKAGE_NAME "libmaxminddb"
#define PACKAGE_STRING "libmaxminddb ${PKG_VER}"
#define PACKAGE_TARNAME "libmaxminddb"
#define PACKAGE_VERSION "${PKG_VER}"
#define VERSION "${PKG_VER}"
